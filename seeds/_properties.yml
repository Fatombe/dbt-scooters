version: 2

seeds:
  - name: "models"
    description: "Scooter models info and statistics"
    config:
      delimiter: ","
      --column_types:
        --scooters : varchar(10)
  - name: "event_types"
    description: ""
    config:
        delimeter: ","
  - name: "age_groups"
    description: "Age groups to split users for analysis"