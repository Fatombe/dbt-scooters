version: 2

seeds:
  - name: "models"
    description: "Scooter models info and statistics"
    config:
      delimiter: ","
      --column_types:
        --scooters : varchar(10)
  - name: "event_types"
    description: ""
    config:
        delimeter: ","
        